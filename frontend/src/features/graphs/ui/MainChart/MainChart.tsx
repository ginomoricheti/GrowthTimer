
const MainChart = () => {
  return (
    <div>
      {/* {data.map(item => (
        <div key={item.project_name}>
          <h3>{item.project_name}</h3>
          <p>Minutos: {item.total_minutes}</p>
          <p>Tareas únicas: {item.unique_tasks}</p>
        </div>
      ))} */}
    </div>
  )
}

export default MainChart;